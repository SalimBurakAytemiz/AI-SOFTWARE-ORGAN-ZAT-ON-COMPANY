# demo-service (fixture)

A disposable HTTP service used only by the Agent Runtime proof workflow
(`ai-company proof`). It is **not a product** and is never deployed anywhere.

The proof task — "Add a GET /health endpoint to the demo service" — gives the
runtime a concrete, safe piece of feature work to route through the
feature-development lifecycle up to the Human Founder approval gate.
